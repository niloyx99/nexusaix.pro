# Backend API URL — frontend calls this for /api/*
VITE_BACKEND_URL=http://localhost:7777

# Render production example:
# VITE_BACKEND_URL=https://your-backend.onrender.com

# Same-origin bundle (UI served by backend) — leave empty at build time:
# VITE_BACKEND_URL=
